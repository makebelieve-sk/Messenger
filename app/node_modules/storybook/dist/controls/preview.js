// src/controls/preview.ts
import { definePreview as e } from "storybook/preview-api";
var f = e({
  // Controls addon doesn't need any preview-side configuration
  // It operates entirely through the manager UI
});
export {
  f as default
};
