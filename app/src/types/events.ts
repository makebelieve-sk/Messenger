enum MainClientEvents {
    REDIRECT = "redirect",
    GET_ME = "get-me",
    ERROR = "error",
};

enum UserDetailsEvents {
    UPDATE = 'update'
}

export {
    MainClientEvents,
    UserDetailsEvents
};